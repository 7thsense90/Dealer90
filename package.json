{
  "name": "dealer90-platform",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/api & npm run dev -w apps/web",
    "build": "npm run db:migrate -w apps/api && npm run build -w apps/web",
    "test": "npm test -w apps/api"
  },
  "type": "module",
  "engines": {
    "node": "22.x"
  }
}
